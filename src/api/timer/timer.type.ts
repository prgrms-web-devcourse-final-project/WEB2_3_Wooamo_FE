/* eslint-disable @typescript-eslint/no-unused-vars */
interface TimerCategoryType {
  timerId: number;
  categoryId: number;
  name: string;
  studyDate: string;
  studyTime: string;
}

interface studyTimeType {
  studyTime: string;
  studyDate: string;
}
